## 1. Establish the compilable transition seam

- [x] 1.1 Add only the `x-sync` legacy-transition domain types, closed source/version and outcome enums, parser-version constant, repository traits, and `NotImplemented` entry points needed by the tests below; add the `ratatoskr-x-transition` binary target with the five closed subcommands but no successful behavior. No RED test precedes this task because Rust behavior tests must compile and fail on an assertion rather than on missing symbols. Verify the seam with `build-gate -- cargo check --locked -p ratatoskr-x-sync -p ratatoskr-x`.

## 2. Persist secret-free legacy transition evidence

- [x] 2.1 RED — extend `crates/x-persistence/tests/schema.rs` with `legacy_transition_tables_are_account_scoped_and_secret_free` and `legacy_imported_posts_allow_unresolved_authors_without_fabricating_users`; run `build-gate -- cargo test --locked -p ratatoskr-x-persistence --test schema legacy_transition_tables_are_account_scoped_and_secret_free -- --exact` and `build-gate -- cargo test --locked -p ratatoskr-x-persistence --test schema legacy_imported_posts_allow_unresolved_authors_without_fabricating_users -- --exact`, and confirm both reach their catalog assertions and fail because the current schema lacks the four tables and nullable imported-author representation.
- [x] 2.2 GREEN — edit the one current `crates/x-persistence/schema.sql` definition in place to add `legacy_import_runs`, `legacy_import_items`, `legacy_shadow_reports`, and `legacy_transition_approvals`, their account/source/parser/digest/idempotency constraints, and nullable imported post author linkage; update the exact owned-table inventory and repository records without adding migrations, secrets, bodies, handles, raw URLs, or source paths. Re-run both exact tests from 2.1 and the complete `schema` test target through `build-gate`, and confirm they pass.

## 3. Preflight pinned legacy source formats safely

- [x] 3.1 RED — add redacted public-source-derived fixtures and `crates/x-sync/tests/legacy_sources.rs::preflight_accepts_pinned_monolith_csv_field_theory_jsonl_and_sqlite_v6`; run `build-gate -- cargo test --locked -p ratatoskr-x-sync --test legacy_sources preflight_accepts_pinned_monolith_csv_field_theory_jsonl_and_sqlite_v6 -- --exact`, and confirm the assertion fails with `NotImplemented` while checking exact schemas, versions, bounded rows, deterministic digests, and JSONL precedence.
- [x] 3.2 GREEN — implement bounded adapters for the exact nine-column monolith CSV, Field Theory JSONL v1, and Field Theory SQLite v6 37-column projection; reuse pinned SQLx with SQLite enabled only after documenting its maintenance, security, BSD/MIT/Apache compatibility, and build impact, open SQLite read-only without creation, and prefer JSONL when both artifacts are selected. Re-run the exact test from 3.1 and confirm it passes.
- [x] 3.3 RED — add `crates/x-sync/tests/legacy_sources.rs::credential_bearing_or_writable_source_is_rejected_without_mutation`; run its exact `build-gate -- cargo test --locked -p ratatoskr-x-sync --test legacy_sources ... -- --exact` command, and confirm the assertion fails while proving target/source digests are unchanged and only the rejected field name is surfaced.
- [x] 3.4 GREEN — implement full-shape allow-listing, credential/session field-name rejection, byte/row/text/JSON-depth limits, duplicate-key conflict detection, source-before/after digest checks, and validation-before-target-write behavior. Re-run the exact test from 3.3 and the full `legacy_sources` test target through `build-gate`, and confirm they pass.

## 4. Import truthful, owner-verified observations atomically

- [x] 4.1 RED — add `crates/x-sync/tests/legacy_import.rs::matching_current_oauth_identity_imports_legacy_observations_atomically` and `mismatched_or_ambiguous_owner_mapping_leaves_target_unchanged` using a hand-written `CurrentAccountIdentity` fake; run each exact test through `build-gate -- cargo test --locked -p ratatoskr-x-sync --test legacy_import <name> -- --exact`, and confirm the matching case fails with `NotImplemented` while mismatch, missing approval, and unavailable identity assertions detect any target write or inferred account.
- [x] 4.2 GREEN — implement approval binding to target account/internal owner/source digest, current `/2/users/me` identity verification through the account's existing OAuth credential seam, and a single PostgreSQL transaction for the accepted run; production code must never inspect a source credential or automatically choose an account. Re-run both exact tests from 4.1 and confirm they pass.
- [x] 4.3 RED — add `crates/x-sync/tests/legacy_import.rs::imports_post_identity_parser_version_and_legacy_provenance_without_fabricating_author` and `conflicting_url_identity_is_retained_without_authoritative_bookmark_mutation`; run each exact test through `build-gate`, and confirm assertions fail on missing post/evidence mapping or on a fabricated/authoritative write.
- [x] 4.4 GREEN — map stable provider post IDs to normalized posts with the importer parser-version stamp, `unknown` availability, distinct publication/observation timestamps, and `legacy-import` provenance; retain legacy categories/folders only as legacy metadata, leave author identity unresolved when only a handle exists, and retain explicit/URL ID conflicts without changing bookmark authority or emitting outbox facts. Re-run both exact tests from 4.3 and confirm they pass.
- [x] 4.5 RED — add `crates/x-sync/tests/legacy_import.rs::repeating_same_fixture_import_is_idempotent_and_invalid_batch_is_atomic`; run the exact test through `build-gate`, and confirm its count, uniqueness, and rollback assertions fail against the first implementation.
- [x] 4.6 GREEN — key completed evidence by account/source kind/source record/source digest/importer parser version, reuse an identical completed run, compute deterministic inserted/matched/updated/conflicted/rejected/unmapped counts, and roll back the whole batch on validation or persistence failure. Re-run the exact test from 4.5 and the full `legacy_import` target through `build-gate`, and confirm they pass.

## 5. Compare only complete official snapshots in shadow mode

- [x] 5.1 RED — add `crates/x-sync/tests/legacy_shadow.rs::shadow_report_classifies_differences_deterministically_without_mutating_bookmarks`; run the exact test through `build-gate -- cargo test --locked -p ratatoskr-x-sync --test legacy_shadow ... -- --exact`, and confirm it fails with `NotImplemented` while asserting stable ordering/digest, all diff classes and orthogonal content/category/folder flags, redaction, and an unchanged bookmark projection.
- [x] 5.2 GREEN — implement account-scoped comparison by provider ID with only unambiguous canonical-URL secondary matches, canonical JSON output, scoped non-secret references, persisted input/report digests, and no reconciliation or outbox side effects. Re-run the exact test from 5.1 and confirm it passes.
- [x] 5.3 RED — add `crates/x-sync/tests/legacy_shadow.rs::incomplete_snapshot_cannot_produce_reviewable_shadow_report`; run the exact test through `build-gate`, and confirm partial, failed, cancelled, truncated, rate-limited, schema-invalid, and cross-account inputs currently violate the refusal/no-report assertions.
- [x] 5.4 GREEN — require a complete successful authoritative snapshot for the same account and refuse every non-authoritative or mismatched input without persisting a clean/reviewable report. Re-run the exact test from 5.3 and the full `legacy_shadow` target through `build-gate`, and confirm they pass.

## 6. Expose the bounded operator workflow and approval gate

- [x] 6.1 RED — add `services/x/tests/transition_cli.rs::transition_cli_never_accepts_credential_or_session_arguments`; run its exact test through `build-gate -- cargo test --locked -p ratatoskr-x --test transition_cli ... -- --exact`, and confirm the command-parser assertion fails unless only `preflight`, `import`, `shadow-report`, `checklist`, and `record-approval` plus non-secret local arguments are accepted.
- [x] 6.2 GREEN — wire those five commands to the application seams with existing typed database/OAuth configuration, non-sensitive output, explicit local source/report/approval paths, and no HTTP route, network cutover, credential/session argument, or persisted/logged source path. Re-run the exact test from 6.1 and confirm it passes.
- [x] 6.3 RED — add `services/x/tests/transition_cli.rs::checklist_and_matching_approval_are_digest_bound_and_stale_evidence_revokes_reviewability`; run the exact test through `build-gate`, and confirm checklist sections, exact owner evidence, supersession, evidence retention, and no-routing-change assertions fail with `NotImplemented`.
- [x] 6.4 GREEN — generate deterministic Markdown covering backup, counts, complete snapshot, shadow findings, privacy/cost review, workspace changeset, stability window, rollback commands, and evidence retention; record explicit owner approval only for the exact current digests and supersede it when any bound input changes, without deleting evidence or activating external routing. Re-run the exact test from 6.3 and the full `transition_cli` target through `build-gate`, and confirm they pass.

## 7. Document the real-data and cross-repository boundary

- [x] 7.1 Update `docs/research/field-theory-source-discovery.md` with pinned upstream evidence and add `docs/cutover/field-theory.md` with the executable preflight/import/shadow/checklist sequence, owner approval inputs, privacy rules, rollback notes, and the separate `ratatoskr-workspace` changeset requirement. No RED test applies because these are operator documents; verify every command against `--help` and ensure examples contain only synthetic IDs and paths.
- [x] 7.2 Run the documented workflow on every committed synthetic fixture in a disposable PostgreSQL database, import each fixture twice, generate the canonical shadow report and checklist, and record the exact inserted/matched/conflicted/unmapped and digest evidence in the test assertions or committed golden files. No separate RED test applies because tasks 3–6 already define the failing behavioral coverage; this task is their end-to-end fixture acceptance.
- [x] 7.3 Stop before any real import or cutover if the owner has not supplied a read-only archive, explicit target account mapping, current OAuth identity, accepted shadow findings, and approval for the exact checklist digest. When supplied, run only the reviewed local workflow; do not read token/session material, delete source/evidence, or change fleet routing from this repository. No RED test applies because this is the human/external approval gate.

## 8. Complete validation and OpenSpec lifecycle

- [x] 8.1 Review the implementation diff for source immutability, secret exclusion, account scoping, timestamp truthfulness, category/folder separation, official full-snapshot authority, deterministic reports, no events/routes/external writes, and absence of migrations or second-version artifacts.
- [x] 8.2 With the disposable PostgreSQL 17 ICU fixture and `X_TEST_DATABASE_URL` configured, run the full local gate exactly as `DEVELOPMENT.md` documents: `build-gate -- cargo fetch --locked`, `build-gate -- cargo deny --locked check`, `cargo fmt --all -- --check`, `build-gate -- cargo clippy --workspace --all-targets --locked -- -D warnings`, `build-gate -- cargo build --workspace --locked`, `build-gate -- cargo test --workspace --locked`, and `build-gate -- cargo build --workspace --locked --release`; do not bypass the shared gate or exceed four jobs.
- [ ] 8.3 Run `git diff --check`, `openspec validate --all --strict`, and `openspec validate --archived`; inspect the exact intended staged diff, then sync/archive this change only after every task is checked and all product gates are green.
- [ ] 8.4 Commit only intended paths on `task/field-theory-import-shadow-cutover`, integrate the exact green commit into `main`, push `main`, verify the remote SHA and hosted checks when available, then remove the dedicated worktree and task branch while preserving the source worktree's unrelated `crates/x-normalize/tests/properties.proptest-regressions` file.
