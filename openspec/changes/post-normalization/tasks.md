# Tasks: post-normalization

Every behaviour task is a pair: the first task adds a test that fails for the stated assertion (minimal compiling stubs are allowed so the failure is an assertion, not a compile error), the second makes it pass. A task that cannot start from a failing test states why in one line. Schema tasks in section 2 require PostgreSQL at `X_TEST_DATABASE_URL`.

## 1. Workspace and dependencies

- [x] 1.1 Add crate `crates/x-normalize` (empty lib skeleton inheriting workspace lints, eighth workspace member) and the exactly-pinned dev-dependency `proptest` in `[workspace.dependencies]`. Verify `cargo metadata --no-deps` lists eight members and `cargo deny --locked check` accepts the tree. Cannot start from a failing test: manifest and dependency configuration.
- [x] 1.2 RED: add `crates/x-normalize/tests/skeleton.rs::crate_exposes_normalizer_surface` asserting the public surface exists — the envelope input type deserializes an empty-but-valid document and the normalizer entry point rejects it with the typed empty-input error. Make it fail first by exposing only a lib-level placeholder module with no such types. Note: the placeholder-stub RED ran as 3.2's decode-refusing stub instead, and the surface assertions settled in `tests/fixtures.rs`; this task's distinctive assertion — an empty-but-valid document refusing with `MissingMember { envelope, data }` — landed as `tests/skeleton.rs::empty_envelope_document_is_refused_with_typed_error`, which passes against the implemented behavior and is retained as the guard.

## 2. Schema extension (`x_archive`, edited in place)

- [x] 2.1 RED: add `crates/x-persistence/tests/schema.rs::posts_carry_conversation_counts_and_parser_columns`: querying the catalog must find `posts.conversation_provider_id text`, nullable bigint columns `like_count retweet_count reply_count quote_count bookmark_count impression_count`, and non-nullable integer `posts.parser_version`; confirm it fails because none exist.
- [x] 2.2 GREEN: extend root `schema.sql` adding those posts columns (counts nullable, parser_version NOT NULL); the test passes.
- [x] 2.3 RED: add `crates/x-persistence/tests/schema.rs::normalization_targets_stamp_parser_version`: querying the catalog must find non-nullable integer `parser_version` on `users`, `post_relations`, and `media`; confirm it fails because the columns do not exist.
- [x] 2.4 GREEN: add the three `parser_version integer NOT NULL` columns; the test passes.

## 3. Committed fixtures

- [x] 3.1 Record synthetic envelopes under `fixtures/x-api/` shaped strictly after the official X API v2 documentation: `tweet_single.json` (one post plus author), `thread.json` (root plus two replies sharing conversation linkage), `quote_dangling_target.json` (quoting post whose target is absent from the envelope), `retweet.json`, `media_post.json` (photo attachment with dimensions and alt text; video member for duration), `article_backed_longform.json` (long-form note content alongside short text, article wrapper riding preservation), `deleted_author.json` (post whose author id has no matching user object), `unknown_field.json` (unmodeled members on post, user, and media objects), `missing_id.json` and `bad_timestamp.json` (structural violations). Placeholder ids and texts only, no real account data. Cannot start from a failing test: recorded payload evidence.
- [x] 3.2 RED: add `crates/x-normalize/tests/fixtures.rs::every_committed_fixture_deserializes_into_the_envelope_type` decoding each file in section 3.1 through the DTO layer; structural refusals (`missing_id.json`, `bad_timestamp.json`) are asserted to fail envelope validation at the boundary type level they violate, all others decode cleanly. Make it fail first against a stub envelope type that decodes nothing.

## 4. DTO layer with record-and-preserve

- [x] 4.1 RED: add `crates/x-normalize/tests/dto.rs::unmodeled_members_are_preserved_per_object`: decoding `unknown_field.json` must expose each injected unknown member, name and value intact, on the corresponding post, user, and media object view. Make it fail with DTO structs that drop unrecognized keys. Note: the first run failed on a wrong assertion (`location` is a modeled member, not an unmodeled one); the fixture gained genuinely unmodeled members and the assertion was corrected before green. Preservation itself had already landed with 4.2 because 3.2's GREEN required the real DTO shape, so the drop-keys failure mode this pair targets never existed at run time; both tests are retained as regression guards over D12.
- [x] 4.2 GREEN: implement serde DTOs mirroring the documented envelope — post objects (ids, text, long-form note linkage, timestamps, language, metrics, conversation id, references, edit metadata, attachments), user objects, media objects — each carrying its extension map via flatten capture; the test passes.
- [x] 4.3 GUARD, cannot start from a failing test because it pins absence of accidental strictness rather than new behaviour: add `crates/x-normalize/tests/dto.rs::envelope_decoding_tolerates_extra_envelope_members` proving an unmodeled top-level envelope member neither fails decoding nor leaks into records except as preserved material.

## 5. Normalizers

- [x] 5.1 RED: add `crates/x-normalize/tests/post.rs::single_tweet_yields_author_and_post_with_content_and_linkage`: normalizing `tweet_single.json` emits exactly one author record and one post record carrying provider ids, canonical text, language, parsed publication timestamp, present metric counts, and conversation linkage equal to the payload value. Make it fail with a normalizer entry point returning the typed empty-output error.
- [x] 5.2 GREEN: implement post+author normalization (author resolution batch-wide, strict timestamp parsing, verbatim conversation copy); the test passes.
- [x] 5.3 RED: add `crates/x-normalize/tests/thread.rs::thread_replies_carry_reply_relations_and_shared_conversation`: normalizing `thread.json` yields reply relations naming each parent provider id, identical conversation linkage across thread members, and no reply relation on the root. Make it fail with relation emission returning an empty set.
- [x] 5.4 GREEN: implement reference mapping to relations with deterministic `(related_post_provider_id, relation)` ordering; the test passes.
- [x] 5.5 RED: add `crates/x-normalize/tests/relation.rs::dangling_quote_target_still_records_relation`: normalizing `quote_dangling_target.json` succeeds and emits the quote relation keyed by the absent target's provider id; `retweet.json` yields the repost variant. Make it fail with a normalizer that drops references to posts outside the envelope. Note: both fixtures passed on their first run because 5.4's mapping is keyed by provider ids and never consults envelope membership, so the drop-references failure mode this pair targets cannot arise in the implemented shape; the tests are retained as regression guards over D6.
- [x] 5.6 GREEN: emit relations independent of envelope membership and map the retweeted type to repost; the test passes. The same cycle added `unrecognized_reference_type_is_preserved_without_a_relation`, pinning design D15's reserved-key preservation for reference types outside the vocabulary.
- [x] 5.7 RED: add `crates/x-normalize/tests/media.rs::attachments_yield_metadata_without_blob_references`: normalizing `media_post.json` emits media records keyed by provider key with kind photo/video, dimensions, alt text, video duration, linked to their owning post's provider id, and every blob reference unset. Make it fail with media emission returning nothing.
- [x] 5.8 GREEN: implement media metadata normalization (metadata only, no byte acquisition anywhere in the crate); the test passes.
- [x] 5.9 RED: add `crates/x-normalize/tests/longform.rs::longform_note_lands_in_long_text_and_edit_time_stays_unset`: normalizing `article_backed_longform.json` keeps canonical text unchanged, carries note content in the long-text field, preserves the edit-history identifiers, and leaves the edit timestamp unset because the payload documents none. Make it fail with a stub that copies note text over the canonical field. Note: passed on its first run because 5.2 already mapped note text into `long_text` beside canonical text and 4.2's extension capture holds the article wrapper; no copy-over stub ever existed, so the pair is retained as the regression guard over D10/D11.
- [x] 5.10 GREEN: map long-form note content to the long-text field and preserve edit metadata without inventing timestamps; the test passes.
- [x] 5.11 RED: add `crates/x-normalize/tests/refusal.rs::unresolved_author_refuses_whole_envelope`: normalizing `deleted_author.json` returns the typed unresolved-author error naming the affected post's provider id, and emits no author, post, relation, or media records. Make it fail with a stub that synthesizes a placeholder author instead of refusing. Note: passed on its first run because 5.2's batch-wide author resolution refuses through the typed error by construction; no placeholder-author stub ever existed, so the test is retained as the regression guard over D5.
- [x] 5.12 GREEN: implement atomic batch refusal on unresolved authors; the test passes.
- [x] 5.13 RED: add `crates/x-normalize/tests/refusal.rs::structural_violations_are_typed_errors`: `missing_id.json` and `bad_timestamp.json` produce distinct typed errors naming the offending object and member, never panics. Make it fail with a stub that treats any decoded object as valid. Note: passed on its first run because validation and strict timestamp parsing landed with the 3.2/5.2 implementations; retained as the regression guard over D12's refusal half.
- [x] 5.14 GREEN: enforce required-member presence and strict timestamp parsing behind the typed error set; the test passes.
- [x] 5.15 GUARD, cannot start from a failing test because it pins existing ordering discipline rather than new behaviour: add `crates/x-normalize/tests/determinism.rs::repeated_normalization_is_identical_across_runs`: normalizing every section 3 fixture twice in-process yields byte-identical serialized record sets, and record collections emerge sorted per design D6 regardless of input order.

## 6. Parser-version stamping

- [x] 6.1 RED: add `crates/x-normalize/tests/parser_version.rs::every_record_carries_active_stamp`: after normalizing every fixture, each emitted record's stamp equals the active crate constant. Make it fail with records defaulting the stamp to zero. Note: passed on its first run because every record constructor stamps `PARSER_VERSION` at the field level since 5.2; no zero-defaulting shape ever existed, so the test is retained as the regression guard over D4.
- [x] 6.2 GREEN: stamp the constant into every record at construction; the test passes.
- [x] 6.3 RED: add `crates/x-normalize/tests/parser_version.rs::round_trip_preserves_the_stamp`: serializing normalized records to JSON and deserializing them again must return stamps equal to the originals for every record kind. Make it fail with a serialization shape that omits the stamp field. Note: passed on its first run because the stamp is an ordinary serde-visible field on every record since its introduction; retained as the regression guard.
- [x] 6.4 GREEN: include the stamp in record serde shapes; the test passes.

## 7. Property-based tests

- [x] 7.1 RED: add `crates/x-normalize/tests/properties.rs::shape_variations_never_panic_and_fail_only_typed`: proptest-generated variations over the fixture grammar (optional members omitted, arrays reordered or emptied, unicode text) always normalize successfully or fail with a typed error, never panic. Make it fail with a strategy harness wired against a normalizer stub that panics on any variation. Note: passed on its first run because the implemented normalizers contain no panicking paths (`unwrap_or_default` handling, no indexing, validated parsing), so no panic-stub ever existed; retained as the regression guard.
- [x] 7.2 GREEN: harden the normalizers until the property holds; the test passes.
- [x] 7.3 RED: add `crates/x-normalize/tests/properties.rs::injected_unknown_keys_always_survive`: arbitrary unknown keys injected into any known object surface intact in that record's preserved material. Make it fail against DTOs that preserve only hand-listed keys. Note: passed on its first run because D2's flatten capture generalizes preservation by construction rather than through hand-listed keys; retained as the regression guard.
- [x] 7.4 GREEN: generalize preservation capture so the property holds; the test passes.
- [x] 7.5 GUARD, cannot start from a failing test because determinism under variation is already pinned by 5.15 at fixture level and this repeats it combinatorially: add `crates/x-normalize/tests/properties.rs::varied_shapes_normalize_deterministically` asserting generated inputs normalize identically across repeated runs.

## 8. Gate, documentation, archive, integration

- [x] 8.1 Update `README.md` (status paragraph and milestone wording: normalization implemented; sync items remain) and `DEVELOPMENT.md` (toolchain paragraph mention of the normalization crate; gate list unchanged and still byte-identical to ci.yml). Cannot start from a failing test: documentation.
- [x] 8.2 Run the full local gate with the disposable Postgres container and capture evidence: `git diff --check`, `openspec validate --all --strict`, `cargo fetch --locked`, `cargo deny --locked check`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, file-length ratchet command, `cargo build --workspace --locked`, `cargo test --workspace --locked`, `cargo build --workspace --locked --release`.

Evidence (2026-08-26): `git diff --check` clean; `openspec validate --all --strict` 5/5 passed; deny advisories/bans/licenses/sources ok; fmt clean after the clippy-driven rewrites; clippy `-D warnings` clean workspace-wide (fixed four redundant closures plus an unfulfilled lint expectation in `normalize.rs`, rewrote `tests/properties.rs` onto `prop::sample::select` so no indexing exists, collapsed one `if`); ratchet found no file over 850 lines; debug build ok; `cargo test --workspace` 44 test binaries all `ok. … 0 failed` against PostgreSQL 17 at `X_TEST_DATABASE_URL`; release build ok.
- [ ] 8.3 With every box above ticked, commit the branch (Conventional Commits), archive the change via OpenSpec, and verify `openspec validate --archived`.
- [ ] 8.4 Integrate: merge the branch into `main`, push `main` to the remote, delete the worktree and the task branch.
